language: node_js
cache:
  directories:
    - ~/.npm
notifications:
  email: false
branches:
  only:
    - master
node_js:
  - 'node'
  - '10'
  - '8'
jobs:
  include:
    - stage: validate
      script: npm run validate
    - stage: deploy
      script: npm run deploy
stages:
  validate
  deploy

