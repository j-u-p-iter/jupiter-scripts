language: node_js
notifications:
  email: false
branches:
  only:
    - master
node_js:
  - '8'
jobs:
  include:
    - stage: validate
      script: node src validate
    - stage: deploy
      script: node src deploy
stages:
  validate
  deploy

