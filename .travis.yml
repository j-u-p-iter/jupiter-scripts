language: node_js
notifications:
  email: false
branches:
  only:
    - master
jobs:
  include:
    - stage: validate
      script: npm run validate
    - stage: deploy
      script: npm run deploy
stages:
  validate
  deploy

